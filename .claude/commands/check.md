# Run Checks

Run all pre-commit checks to ensure code quality.

## Commands

```bash
# Lint and format (auto-fix)
bun check

# Just lint (no auto-fix)
bun lint

# Just format
bun format

# Type check
bun typecheck
```

Run `bun check` now and report any issues that need manual attention.
